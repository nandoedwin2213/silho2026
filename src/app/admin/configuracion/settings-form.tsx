"use client";

import { useState } from "react";

type Values = { discount: string; webAssessmentDiscount: string; webTreatmentBonus: string; webBonusDays: string; webOfferUntil: string; rewardsPerUsd: string; rewardsPointValue: string; rewardsMaxRedeem: string; rewardsExpiryMonths: string; clinicHours: string; whatsapp: string; instagram: string; tiktok: string; facebook: string; surgical: boolean; email: string; instructions: string };

export function SettingsForm({ initial }: { initial: Values }) {
  const [values, setValues] = useState(initial);
  const [message, setMessage] = useState("");
  const update = (key: keyof Values, value: string | boolean) => setValues((current) => ({ ...current, [key]: value }));
  async function submit(event: React.FormEvent) {
    event.preventDefault();
    const response = await fetch("/api/admin/settings", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...values, discount: Number(values.discount) }) });
    setMessage(response.ok ? "Configuración guardada." : "No se pudo guardar la configuración.");
  }
  return <form onSubmit={submit} className="grid gap-5 rounded-2xl border bg-white p-6 shadow-sm md:grid-cols-2"><label className="text-sm">Descuento web anterior<input name="discount" type="number" min="0" max="50" value={values.discount} onChange={(event) => update("discount", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Descuento web valoración<input name="webAssessmentDiscount" type="number" min="0" max="50" value={values.webAssessmentDiscount} onChange={(event) => update("webAssessmentDiscount", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Bono web (USD)<input name="webTreatmentBonus" value={values.webTreatmentBonus} onChange={(event) => update("webTreatmentBonus", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Días del bono<input name="webBonusDays" value={values.webBonusDays} onChange={(event) => update("webBonusDays", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Oferta válida hasta<input name="webOfferUntil" value={values.webOfferUntil} onChange={(event) => update("webOfferUntil", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Puntos por USD<input name="rewardsPerUsd" value={values.rewardsPerUsd} onChange={(event) => update("rewardsPerUsd", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Valor de 1 punto (USD)<input name="rewardsPointValue" value={values.rewardsPointValue} onChange={(event) => update("rewardsPointValue", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Máximo de canje (%)<input name="rewardsMaxRedeem" value={values.rewardsMaxRedeem} onChange={(event) => update("rewardsMaxRedeem", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Vigencia de puntos (meses)<input name="rewardsExpiryMonths" value={values.rewardsExpiryMonths} onChange={(event) => update("rewardsExpiryMonths", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm md:col-span-2">Horarios de clínica<input name="clinicHours" value={values.clinicHours} onChange={(event) => update("clinicHours", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">WhatsApp<input name="whatsapp" value={values.whatsapp} onChange={(event) => update("whatsapp", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Instagram<input name="instagram" value={values.instagram} onChange={(event) => update("instagram", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">TikTok<input name="tiktok" value={values.tiktok} onChange={(event) => update("tiktok", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Facebook<input name="facebook" value={values.facebook} onChange={(event) => update("facebook", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="text-sm">Correo de clínica<input name="email" type="email" value={values.email} onChange={(event) => update("email", event.target.value)} className="mt-2 h-9 w-full rounded-lg border px-3" /></label><label className="flex items-center gap-3 text-sm"><input type="checkbox" name="surgical" checked={values.surgical} onChange={(event) => update("surgical", event.target.checked)} /> Mostrar precios quirúrgicos</label><textarea name="instructions" value={values.instructions} onChange={(event) => update("instructions", event.target.value)} rows={4} placeholder="Instrucciones de transferencia" className="rounded-lg border p-3 text-sm md:col-span-2" /><button type="submit" className="rounded-full bg-navy px-5 py-2 text-sm font-semibold text-white md:col-span-2">Guardar configuración</button>{message && <p className="text-sm text-muted-foreground md:col-span-2">{message}</p>}</form>;
}
